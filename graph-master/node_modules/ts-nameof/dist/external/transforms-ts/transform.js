"use strict";
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (Object.hasOwnProperty.call(mod, k)) result[k] = mod[k];
    result["default"] = mod;
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
var ts = __importStar(require("typescript"));
var common_1 = require("./external/common");
function transform(node) {
    switch (node.kind) {
        case "StringLiteral":
            return ts.createLiteral(node.value);
        case "ArrayLiteral":
            return ts.createArrayLiteral(node.elements.map(function (element) { return transform(element); }));
        default:
            return common_1.throwError("Unsupported node kind: " + node.kind);
    }
}
exports.transform = transform;
